import { api, rows } from "../shared/api.js"; import { mountNavigation } from "../shared/navigation.js";
mountNavigation(); const target = document.querySelector("#restaurants"); document.querySelector("#restaurant-search").addEventListener("submit", async event => { event.preventDefault(); try { const items = rows(await api.restaurants.list(new FormData(event.currentTarget).get("city"))); target.innerHTML = items.length ? items.map(item => `<article class="result-card"><h3>${item.name}</h3><p>${item.address || item.location || ""}</p><p>★ ${item.rating || "—"}</p></article>`).join("") : '<div class="empty">No restaurants found.</div>'; } catch (error) { target.innerHTML = `<div class="empty">${error.message}</div>`; } });
