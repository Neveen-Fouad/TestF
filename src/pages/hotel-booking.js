import { api } from "../shared/api.js"; import { mountNavigation, notify, requireLogin } from "../shared/navigation.js";
mountNavigation("hotels"); if (requireLogin()) { const form = document.querySelector("#booking-form"); form.elements.hotel_id.value = new URLSearchParams(location.search).get("id") || ""; form.addEventListener("submit", async event => { event.preventDefault(); const button = event.submitter; button.disabled = true; try { const result = await api.hotels.book(Object.fromEntries(new FormData(form))); const booking = result?.data || result; location.assign(`/pages/payments.html?booking_id=${encodeURIComponent(booking.id || booking.booking_id || "")}`); } catch (error) { notify(error.message, true); button.disabled = false; } }); }
