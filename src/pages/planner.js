import { api } from "../shared/api.js"; import { escapeHtml, mountNavigation, notify, requireLogin } from "../shared/navigation.js";
mountNavigation("plan-a-trip"); if (requireLogin()) document.querySelector("#plan").addEventListener("submit", async event => { event.preventDefault(); const form = event.currentTarget; const button = form.querySelector("button"); const values = Object.fromEntries(new FormData(form)); values.number_of_travels = Number(values.number_of_travels); values.number_of_days = Number(values.number_of_days); values.budget = Number(values.budget); values.estimated_expenses = Number(values.estimated_expenses || 0); const endDate = new Date(`${values.start_date}T00:00:00`); endDate.setDate(endDate.getDate() + values.number_of_days); values.end_date = endDate.toISOString().slice(0, 10); button.disabled = true; button.textContent = "Creating your plan…"; try { const response = await api.trips.create(values, true); const trip = response?.data || response; const id = trip?.id || trip?.trip?.id; document.querySelector("#plan-result").innerHTML = `<section class="plan-success"><span>✦</span><div><p class="eyebrow">YOUR PLAN IS READY</p><h2>${escapeHtml(trip?.destination || values.destination)}</h2><p>${escapeHtml(response?.message || "Joy has created the first version of your itinerary.")}</p></div>${id ? `<a class="button subtle" href="/pages/trip-details.html?id=${encodeURIComponent(id)}">Open itinerary</a>` : ""}</section>`; notify("Your trip plan is ready."); } catch (error) { document.querySelector("#plan-result").innerHTML = `<div class="empty">${escapeHtml(error.message)}</div>`; notify(error.message, true); } finally { button.disabled = false; button.textContent = "Create my trip plan"; } });
