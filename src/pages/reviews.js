import { api, rows } from "../shared/api.js"; import { mountNavigation } from "../shared/navigation.js";
mountNavigation(); const target = document.querySelector("#reviews"); try { const items = rows(await api.reviews.list()); target.innerHTML = items.length ? items.map(item => `<article class="result-card"><h3>${item.title || item.destination?.name || "Traveler review"}</h3><p>★ ${item.rating || "—"}</p><p>${item.comment || item.content || ""}</p></article>`).join("") : '<div class="empty">No reviews available.</div>'; } catch (error) { target.innerHTML = `<div class="empty">${error.message}</div>`; }
