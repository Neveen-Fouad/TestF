import { api, rows } from "../shared/api.js"; import { mountNavigation } from "../shared/navigation.js";
mountNavigation("explore"); const target = document.querySelector("#trips"); try { const trips = rows(await api.trips.list()); target.innerHTML = trips.length ? trips.map(trip => `<article class="result-card"><div class="eyebrow">${trip.destination || trip.country || "JOURNEY"}</div><h3>${trip.name || trip.title || "Untitled trip"}</h3><p>${trip.description || trip.duration || "View its live daily itinerary."}</p><a class="button subtle" href="/pages/trip-details.html?id=${encodeURIComponent(trip.id)}">Open trip</a></article>`).join("") : '<div class="empty">No trips are available.</div>'; } catch (error) { target.innerHTML = `<div class="empty">${error.message}</div>`; }
