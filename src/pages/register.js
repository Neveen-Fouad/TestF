import L from "leaflet"; import "leaflet/dist/leaflet.css"; import { api, session } from "../shared/api.js"; import { mountNavigation, notify } from "../shared/navigation.js";
mountNavigation(); const map = L.map("location-map").setView([30.0444, 31.2357], 5); L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", { attribution: "© OpenStreetMap" }).addTo(map); let marker; map.on("click", event => { marker?.remove(); marker = L.marker(event.latlng).addTo(map); document.querySelector('[name="long"]').value = event.latlng.lng; document.querySelector('[name="latittude"]').value = event.latlng.lat; });
document.querySelector("#register-form").addEventListener("submit", async event => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget)); if (values.password !== values.password_confirmation) return notify("Passwords do not match.", true); if (!values.long) return notify("Choose your location on the map.", true); try { await api.auth.register(values); location.assign("/pages/verify-email.html"); } catch (error) { notify(error.message, true); } });
