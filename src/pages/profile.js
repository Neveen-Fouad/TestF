import { api } from "../shared/api.js"; import { mountNavigation, requireLogin, notify } from "../shared/navigation.js";
mountNavigation(); if (requireLogin()) { const form = document.querySelector("#profile-form"); try { const user = (await api.profile.get()).data || {}; ["first_name", "last_name", "email"].forEach(key => form.elements[key].value = user[key] || ""); } catch (error) { notify(error.message, true); } form.addEventListener("submit", async event => { event.preventDefault(); try { await api.profile.update(Object.fromEntries(new FormData(form))); notify("Profile saved."); } catch (error) { notify(error.message, true); } }); }
