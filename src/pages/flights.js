import { api, rows } from "../shared/api.js"; import { mountNavigation } from "../shared/navigation.js";
mountNavigation("flights"); const target = document.querySelector("#flights"); document.querySelector("#flight-search").addEventListener("submit", async event => { event.preventDefault(); try { const items = rows(await api.flights.search(new FormData(event.currentTarget).get("query"))); target.innerHTML = items.length ? items.map(item => `<article class="result-card"><h3>${item.airline || item.name || "Flight"}</h3><p>${item.departure || ""} → ${item.arrival || ""}</p><p>${item.price || ""}</p></article>`).join("") : '<div class="empty">No flights found.</div>'; } catch (error) { target.innerHTML = `<div class="empty">${error.message}</div>`; } });
